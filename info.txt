# crea / executa els contenidors
docker compose up -d

# llista tots els contenidors del 
docker compose ps -a

# atura els contenidors
docker compose stop

# inicia els contenidors
docker compose start

# reinicia els contenidors
docker compose restart

# elimina els contenidors
docker compose down

# accedir a un contenidor
docker exec -it nom_contenidor bash
